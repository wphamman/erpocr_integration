# Open Questions — erpocr_integration

> Deferred calls and unresolved decisions, **each with a named owner** so they don't rot.
> When a question is answered, promote the resolution to an ADR in [DECISIONS.md](DECISIONS.md)
> and mark the row **Resolved → ADR-nnnn**. Companion to the decision register; operating model
> lives in the `architect-session` skill.
>
> **Bootstrap note (2026-07-09):** reconstructed on the first architect session from CHANGELOG,
> CLAUDE.md, handbacks, and per-session memory. DRAFT until ratified.
>
> **Owner legend:** *Willie* = MD, routes/ratifies + deploy-time UI actions · *Starktail* = hosting
> owner, runs tag→deploy · *Cactus* = the second instance's operator · *Architect* = this session's role.

---

| ID | Question / deferred call | Context & why it matters | Owner | Status | Opened |
|---|---|---|---|---|---|
| **Q1** | ~~Deploy the `v1.5.0 → v1.6.0` train; confirm prod version.~~ | **RESOLVED 2026-07-09** (Willie): Star Pops prod (`erp.starpops.co.za`) is at **v1.6.0 (HEAD)** — the `1.5.0→1.6.0` train is deployed and the 2026-07-07 request landed (superseded by v1.6.0). **Driver-shell deploy #1 is unblocked** on the erpocr side. Residual verification that the deploy's config steps ran → **Q2/Q3/Q4**. | Willie + Starktail | **Resolved 2026-07-09** | 2026-07-09 |
| **Q2** | ~~Run **Restore Original Permissions** on `OCR Fleet Slip` on Star Pops.~~ | **RESOLVED 2026-07-09.** Willie ran Customize Form → Restore Original Permissions on SP; verified read-only same day — **Custom DocPerm rows = 0** → falls through to shipped JSON (System Manager pl=1 for `raw_payload` + OCR Fleet Driver both restored), matching Cactus's clean state. The 3 pre-restore rows were all stale/standard (nothing to re-apply). Shadow closed; no longer a deploy-request line. | Willie | **Resolved 2026-07-09** | 2026-07-09 |
| **Q3** | ~~Set `OCR Settings → Import (Actual VAT) Tax Template = "9 - Import with Std VAT"` on Cactus.~~ | **RESOLVED 2026-07-09.** Willie set it; verified read-only same day — Cactus `import_tax_template = "9 - Import with Std VAT"` (default still "1 - Standard VAT"). ADR-0008's customs-VAT fix is now **active on Cactus**; the next Cargo Compass customs invoice should draft with the Actual VAT row pre-filled (→ Q4). | Cactus / Willie | **Resolved 2026-07-09** | 2026-07-09 |
| **Q4** | Confirm auto-draft produces a **correct** draft end-to-end, now that v1.6.0 is live. | **FY-guard fix VERIFIED live 2026-07-09** (read-only probe): **0** Fiscal-Year rejections among the 4 records processed post-deploy (modified ≥ 07-07); the 12 "outside any active Fiscal Year" skips are all **pre-deploy fossils** (last modified ≤ 2026-07-02) and SP has active FYs covering those dates (FY 2026-2027 open). **Still pending:** only **2** `auto_drafted=1` ever; post-deploy volume (winter break) is 4 *low-confidence* records (no-supplier / 'Suggested'), so a *correct high-confidence draft* is still unproven — verify when real volume resumes. Cactus customs-PI side: Q3 set 2026-07-09; no Cargo Compass invoice yet — confirm on first occurrence. **2026-07-10 probe (read-only, authorized):** Cactus volume resumed (11 imports); **first organic auto-draft FIRED** — `OCR-IMP-01918` → draft `ACC-PINV-2026-00446` (supplier/bill_no/dates/items/template all correct) **but overdrafted R132.76**: a 5% invoice discount landed in the extracted subtotal, not the line rates (schema has no discount field) → **Q11 opened** (totals-reconciliation gate); the draft needs manual rate correction at review before submit. A duplicate-bill_no case correctly declined the same day. Verdict: auto-draft fires and is structurally correct; numeric correctness on discounted invoices is the gap Q11 closes. SP still zero volume (break). | Architect + Willie | **Open — firing confirmed; Q11 gates the found error class; Cargo Compass case still pending** | 2026-07-09 |
| **Q5** | ~~Finalize + merge the `starpops_accounts` fold-in (ADR-0010).~~ | **RESOLVED 2026-07-09 → ADR-0010 + ADR-0011 shipped in v1.7.0** (merge `762301d`, tagged + released). Refold-fresh (not rebase); commit-the-dist. Two-pass reviewed; one round bounced 2 basename-nav bugs, fixed + live-walk-verified before merge. The Starktail Node-step flag was eliminated by commit-the-dist (ADR-0011). | Architect + Willie | **Resolved 2026-07-09 (v1.7.0)** | 2026-07-09 |
| **Q6** | Remove the **vestigial `custom_fleet_control_account`** capture on the Fleet Card path. | Since v1.2.0 (ADR-0003) no PI is created on Fleet Card slips, so the control account captured per slip via `_apply_vehicle_config` no longer flows anywhere — informational only. CLAUDE.md flags "cleanup TBD in a future release." Low priority; rode the v1.8.0 sweep. **RESOLVED — shipped v1.8.0** (merge `acdddc9`): Fleet Card path no longer captures it; Direct-Expense flips fall back to `fleet_expense_account` with a create-time guard when unresolvable (bounce fix). The §4a Fleet Vehicle Custom Field itself is untouched. | Architect | **Resolved 2026-07-10 (v1.8.0)** | 2026-07-09 |
| **Q7** | Small deferred review items (non-blocking, from the 2026-07-06 live review). | (a) **Fleet pipeline has no Actual-injection** — an operator manually picking the import template on a fleet slip gets a 0-tax row; (b) **decode-gate only on `upload_fleet_slip`**, not on manual/email/Drive image ingest; (c) **item aliases are globally keyed** — supplier-scoped aliases are the durable fix if cross-supplier description collisions bite; (d) **`getdate()` hardening** on recon date compares. **All four ruled IN 2026-07-09** — (c) included by Willie's explicit call (supplier-scoped tier beats global, existing global aliases stay working as the fallback tier). | Architect | **Resolved 2026-07-10 (v1.8.0 → ADR-0013 for (c))** | 2026-07-09 |
| **Q8** | Is manual **Mark Recorded** the right long-term UX for Fleet Card slips, or should high-confidence ones auto-record? | **34+ Fleet Card slips sat in `Matched`** awaiting manual Mark Recorded as of the 2026-07-06 review (was ~17 at the v1.2.0 audit) — a growing operational backlog. Auto-recording touches the ADR-0003 invariant and the `fleet_management` reconciliation contract, so it was a business/process call. **RULED by Willie 2026-07-09: BOTH levers** — an opt-in auto-record setting (off by default, ADR-0002 style; high confidence = Confirmed vehicle + recon payload present; `purchase_invoice` stays NULL per ADR-0003) AND a server-revalidated bulk Mark Recorded list action. **RESOLVED — shipped v1.8.0** (merge `acdddc9`) **→ ADR-0012.** | Willie (ruled) | **Resolved 2026-07-10 (v1.8.0 → ADR-0012)** | 2026-07-09 |
| **Q9** | Refactor `_build_taxes_from_template` to explicit inputs (kill the `tax_proxy` duck-typing). | The v1.8.0 fleet Actual-VAT path calls the invoice pipeline's tax builder through a `SimpleNamespace` proxy (all six read-attributes covered today — verified at the v1.8.0 gate). If the helper ever reads a NEW attribute, the fleet path AttributeErrors on prod while the invoice path keeps working — and the mocked suite won't see it. Refactor to explicit parameters (`tax_template, company, tax_amount, rates_include_tax, …`) **at the next release that touches the invoice path**; don't churn the shipped path for it alone. **RESOLVED — shipped v1.9.0** (merge `ec4910a`): explicit args (`tax_template, company, tax_amount, rates_include_tax`), proxy deleted, injection semantics proven byte-identical (tests + bench + Codex parity check). | Architect | **Resolved 2026-07-10 (v1.9.0)** | 2026-07-10 |
| **Q10** | Should supplier-derived matching tiers require a CONFIRMED supplier? | Tier 1 (`Item Supplier` lookup, since v1.1.0) and now tier 2 (supplier-scoped aliases, v1.8.0) accept a **fuzzy-`Suggested` supplier** — items can show Auto Matched confidence keyed off a guessed supplier. Contained today: auto-draft is still blocked by the Suggested supplier, so no document is created from the guess; the exposure is displayed match confidence only. Requiring confirmation would change tier-1 behavior too. **RULED by Willie 2026-07-10: cap, don't skip** — supplier-keyed item matches display the MIN of the chain (Suggested supplier → Suggested items), pre-fill kept, both tiers + DN path consistent. **RESOLVED — shipped v1.9.0** (merge `ec4910a`): `_cap_to_supplier` at both supplier-keyed tiers, invoice + DN threaded; label-only (item selection, learning, auto-draft outcomes unchanged — Codex-verified). | Willie (ruled) | **Resolved 2026-07-10 (v1.9.0)** | 2026-07-10 |
| **Q11** | Gate auto-draft on totals reconciliation (line sum vs extracted subtotal). | Opened by the Q4 probe 2026-07-10: the **first organic auto-draft overdrafted R132.76** (`OCR-IMP-01918` → `ACC-PINV-2026-00446`, Cactus) — a 5% invoice discount lived in the extracted subtotal but not the line rates; PI amounts build from qty×rate, so **any globally-discounted invoice systematically overdrafts** (Gemini schema has no discount field). Fix: auto-draft skips (with an amount-naming skip-reason) when Σ(qty×unit_price) deviates from the extracted subtotal beyond tolerance; manual creation untouched (ADR-0002). A schema-level discount field is the bigger design — deferred. **RESOLVED — shipped v1.9.0** (merge `ec4910a`) **→ ADR-0014** (incl. the tax-inclusivity coupling: inclusive rates reconcile against total, not subtotal). | Architect | **Resolved 2026-07-10 (v1.9.0 → ADR-0014)** | 2026-07-10 |
