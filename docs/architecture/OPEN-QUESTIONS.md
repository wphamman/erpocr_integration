# Open Questions — erpocr_integration

> Deferred calls and unresolved decisions, **each with a named owner** so they don't rot.
> When a question is answered, promote the resolution to an ADR in [DECISIONS.md](DECISIONS.md)
> and mark the row **Resolved → ADR-nnnn**. Companion to the decision register; operating model
> lives in the `architect-session` skill.
>
> **Bootstrap note (2026-07-09):** reconstructed on the first architect session from CHANGELOG,
> CLAUDE.md, handbacks, and per-session memory. DRAFT until ratified.
>
> **Owner legend:** *Willie* = MD, routes/ratifies + deploy-time UI actions · *Starktail* = hosting
> owner, runs tag→deploy · *Cactus* = the second instance's operator · *Architect* = this session's role.

---

| ID | Question / deferred call | Context & why it matters | Owner | Status | Opened |
|---|---|---|---|---|---|
| **Q1** | ~~Deploy the `v1.5.0 → v1.6.0` train; confirm prod version.~~ | **RESOLVED 2026-07-09** (Willie): Star Pops prod (`erp.starpops.co.za`) is at **v1.6.0 (HEAD)** — the `1.5.0→1.6.0` train is deployed and the 2026-07-07 request landed (superseded by v1.6.0). **Driver-shell deploy #1 is unblocked** on the erpocr side. Residual verification that the deploy's config steps ran → **Q2/Q3/Q4**. | Willie + Starktail | **Resolved 2026-07-09** | 2026-07-09 |
| **Q2** | ~~Run **Restore Original Permissions** on `OCR Fleet Slip` on Star Pops.~~ | **RESOLVED 2026-07-09.** Willie ran Customize Form → Restore Original Permissions on SP; verified read-only same day — **Custom DocPerm rows = 0** → falls through to shipped JSON (System Manager pl=1 for `raw_payload` + OCR Fleet Driver both restored), matching Cactus's clean state. The 3 pre-restore rows were all stale/standard (nothing to re-apply). Shadow closed; no longer a deploy-request line. | Willie | **Resolved 2026-07-09** | 2026-07-09 |
| **Q3** | ~~Set `OCR Settings → Import (Actual VAT) Tax Template = "9 - Import with Std VAT"` on Cactus.~~ | **RESOLVED 2026-07-09.** Willie set it; verified read-only same day — Cactus `import_tax_template = "9 - Import with Std VAT"` (default still "1 - Standard VAT"). ADR-0008's customs-VAT fix is now **active on Cactus**; the next Cargo Compass customs invoice should draft with the Actual VAT row pre-filled (→ Q4). | Cactus / Willie | **Resolved 2026-07-09** | 2026-07-09 |
| **Q4** | Confirm auto-draft produces a **correct** draft end-to-end, now that v1.6.0 is live. | **FY-guard fix VERIFIED live 2026-07-09** (read-only probe): **0** Fiscal-Year rejections among the 4 records processed post-deploy (modified ≥ 07-07); the 12 "outside any active Fiscal Year" skips are all **pre-deploy fossils** (last modified ≤ 2026-07-02) and SP has active FYs covering those dates (FY 2026-2027 open). **Still pending:** only **2** `auto_drafted=1` ever; post-deploy volume (winter break) is 4 *low-confidence* records (no-supplier / 'Suggested'), so a *correct high-confidence draft* is still unproven — verify when real volume resumes. Cactus customs-PI side: Q3 set 2026-07-09; no Cargo Compass invoice yet — confirm on first occurrence. **2026-07-10 probe (read-only, authorized):** Cactus volume resumed (11 imports); **first organic auto-draft FIRED** — `OCR-IMP-01918` → draft `ACC-PINV-2026-00446` (supplier/bill_no/dates/items/template all correct) **but overdrafted R132.76**: a 5% invoice discount landed in the extracted subtotal, not the line rates (schema has no discount field) → **Q11 opened** (totals-reconciliation gate); the draft needs manual rate correction at review before submit. A duplicate-bill_no case correctly declined the same day. Verdict: auto-draft fires and is structurally correct; numeric correctness on discounted invoices is the gap Q11 closes. SP still zero volume (break). | Architect + Willie | **Open — firing confirmed; Q11 gates the found error class; Cargo Compass case still pending** | 2026-07-09 |
| **Q5** | ~~Finalize + merge the `starpops_accounts` fold-in (ADR-0010).~~ | **RESOLVED 2026-07-09 → ADR-0010 + ADR-0011 shipped in v1.7.0** (merge `762301d`, tagged + released). Refold-fresh (not rebase); commit-the-dist. Two-pass reviewed; one round bounced 2 basename-nav bugs, fixed + live-walk-verified before merge. The Starktail Node-step flag was eliminated by commit-the-dist (ADR-0011). | Architect + Willie | **Resolved 2026-07-09 (v1.7.0)** | 2026-07-09 |
| **Q6** | Remove the **vestigial `custom_fleet_control_account`** capture on the Fleet Card path. | Since v1.2.0 (ADR-0003) no PI is created on Fleet Card slips, so the control account captured per slip via `_apply_vehicle_config` no longer flows anywhere — informational only. CLAUDE.md flags "cleanup TBD in a future release." Low priority; rode the v1.8.0 sweep. **RESOLVED — shipped v1.8.0** (merge `acdddc9`): Fleet Card path no longer captures it; Direct-Expense flips fall back to `fleet_expense_account` with a create-time guard when unresolvable (bounce fix). The §4a Fleet Vehicle Custom Field itself is untouched. | Architect | **Resolved 2026-07-10 (v1.8.0)** | 2026-07-09 |
| **Q7** | Small deferred review items (non-blocking, from the 2026-07-06 live review). | (a) **Fleet pipeline has no Actual-injection** — an operator manually picking the import template on a fleet slip gets a 0-tax row; (b) **decode-gate only on `upload_fleet_slip`**, not on manual/email/Drive image ingest; (c) **item aliases are globally keyed** — supplier-scoped aliases are the durable fix if cross-supplier description collisions bite; (d) **`getdate()` hardening** on recon date compares. **All four ruled IN 2026-07-09** — (c) included by Willie's explicit call (supplier-scoped tier beats global, existing global aliases stay working as the fallback tier). | Architect | **Resolved 2026-07-10 (v1.8.0 → ADR-0013 for (c))** | 2026-07-09 |
| **Q8** | Is manual **Mark Recorded** the right long-term UX for Fleet Card slips, or should high-confidence ones auto-record? | **34+ Fleet Card slips sat in `Matched`** awaiting manual Mark Recorded as of the 2026-07-06 review (was ~17 at the v1.2.0 audit) — a growing operational backlog. Auto-recording touches the ADR-0003 invariant and the `fleet_management` reconciliation contract, so it was a business/process call. **RULED by Willie 2026-07-09: BOTH levers** — an opt-in auto-record setting (off by default, ADR-0002 style; high confidence = Confirmed vehicle + recon payload present; `purchase_invoice` stays NULL per ADR-0003) AND a server-revalidated bulk Mark Recorded list action. **RESOLVED — shipped v1.8.0** (merge `acdddc9`) **→ ADR-0012.** | Willie (ruled) | **Resolved 2026-07-10 (v1.8.0 → ADR-0012)** | 2026-07-09 |
| **Q9** | Refactor `_build_taxes_from_template` to explicit inputs (kill the `tax_proxy` duck-typing). | The v1.8.0 fleet Actual-VAT path calls the invoice pipeline's tax builder through a `SimpleNamespace` proxy (all six read-attributes covered today — verified at the v1.8.0 gate). If the helper ever reads a NEW attribute, the fleet path AttributeErrors on prod while the invoice path keeps working — and the mocked suite won't see it. Refactor to explicit parameters (`tax_template, company, tax_amount, rates_include_tax, …`) **at the next release that touches the invoice path**; don't churn the shipped path for it alone. **RESOLVED — shipped v1.9.0** (merge `ec4910a`): explicit args (`tax_template, company, tax_amount, rates_include_tax`), proxy deleted, injection semantics proven byte-identical (tests + bench + Codex parity check). | Architect | **Resolved 2026-07-10 (v1.9.0)** | 2026-07-10 |
| **Q10** | Should supplier-derived matching tiers require a CONFIRMED supplier? | Tier 1 (`Item Supplier` lookup, since v1.1.0) and now tier 2 (supplier-scoped aliases, v1.8.0) accept a **fuzzy-`Suggested` supplier** — items can show Auto Matched confidence keyed off a guessed supplier. Contained today: auto-draft is still blocked by the Suggested supplier, so no document is created from the guess; the exposure is displayed match confidence only. Requiring confirmation would change tier-1 behavior too. **RULED by Willie 2026-07-10: cap, don't skip** — supplier-keyed item matches display the MIN of the chain (Suggested supplier → Suggested items), pre-fill kept, both tiers + DN path consistent. **RESOLVED — shipped v1.9.0** (merge `ec4910a`): `_cap_to_supplier` at both supplier-keyed tiers, invoice + DN threaded; label-only (item selection, learning, auto-draft outcomes unchanged — Codex-verified). | Willie (ruled) | **Resolved 2026-07-10 (v1.9.0)** | 2026-07-10 |
| **Q11** | Gate auto-draft on totals reconciliation (line sum vs extracted subtotal). | Opened by the Q4 probe 2026-07-10: the **first organic auto-draft overdrafted R132.76** (`OCR-IMP-01918` → `ACC-PINV-2026-00446`, Cactus) — a 5% invoice discount lived in the extracted subtotal but not the line rates; PI amounts build from qty×rate, so **any globally-discounted invoice systematically overdrafts** (Gemini schema has no discount field). Fix: auto-draft skips (with an amount-naming skip-reason) when Σ(qty×unit_price) deviates from the extracted subtotal beyond tolerance; manual creation untouched (ADR-0002). A schema-level discount field is the bigger design — deferred. **RESOLVED — shipped v1.9.0** (merge `ec4910a`) **→ ADR-0014** (incl. the tax-inclusivity coupling: inclusive rates reconcile against total, not subtotal). | Architect | **Resolved 2026-07-10 (v1.9.0 → ADR-0014)** | 2026-07-10 |
| **Q12** | ~~Close ERP-P2-2 with explicit CSRF enforcement and prove the existing driver shell remains compatible.~~ | **RESOLVED 2026-07-13 → ADR-0017 / v1.10.0.** Fresh source Pass R was 0/0/0/0. The sole combined runtime then proved first headerless and wrong-token Website requests denied with zero slip/File/job effects; matching Website CSRF, normal System Driver CSRF, and API-token/no-cookie requests accepted. The real Driver Shell reused one token, client key, and capture time across two controlled 500s and the successful retry, creating exactly one slip and reaching Received. | Architect + driver architect | **Resolved 2026-07-13 (v1.10.0 → ADR-0017)** | 2026-07-13 |
| **Q13** | ~~Close ERP-P2-3 by making supplier statement work discoverable in `/accounts`.~~ | **RESOLVED 2026-07-13 → ADR-0018 / v1.10.0.** Fresh frontend Pass R was 0/0/0/0 and the build reproduced committed dist byte-for-byte. The sole combined browser runtime proved live Pending/Reconciled counts, required list fields, refresh, loading/empty/real-403 error states, invalid-route content bounce, and exact Desk drill-through without a typed direct URL; served JS/CSS matched the candidate. | Architect | **Resolved 2026-07-13 (v1.10.0 → ADR-0018)** | 2026-07-13 |
| **Q14** | Retire the accepted production dependency advisories without destabilizing the release. | ADR-0019 records 2 High + 3 Moderate transitive advisories. Latest SDK still pins the affected Socket.IO client; automatic audit proposes a regressive SDK downgrade. Run as a dedicated post-repair unit with supported versions/overrides evaluated under full build/auth/browser regression. Any ADR-0019 invalidation signal promotes this to a release blocker. | Architect | **Open — accepted-for-release, monitored** | 2026-07-13 |
| **Q15** | Should the auto-draft totals gate (`_totals_reconcile`) become amount-aware like the PI/PR builders? | v1.10.2 (ADR-0021) made the builders derive line rates from the extracted `amount`, but the auto-draft gate still sums raw `qty × rate`. A correctly-extracted DISCOUNTED invoice therefore still fails the gate and routes to human review, even though the draft it would now produce is numerically correct — the gate models the pre-v1.10.2 builder. Left conservative deliberately: making it amount-aware would widen AUTOMATIC invoice creation on live data (`enable_auto_draft` is ON at both instances), which is an owner call, not a hotfix call. The bleed case stays protected either way (Σ amounts 1391.25 vs subtotal 1470.75 diverges). Architect recommendation: leave as-is, revisit as its own change with its own review. | Willie (to rule) + Architect | **Open — deliberate; no action needed for correctness** | 2026-07-27 |
| **Q16** | On v16 the `ERPNext OCR` Desk workspace is unreachable from the Desktop landing — ship `desktop_icon/*.json`? | Coordinator's 2026-09-16 measurement on `erp-test.starpops.co.za` (frappe 16.33.1, a prod copy): the v16 landing is the **Desktop** (`Desktop Icon` rows; `/apps` → 301 `/desk`, so the v1.10.3 kiosk back-link holds). Because our tile route `/accounts` is a portal route, the auto-generated icon for the file-backed `ERPNext OCR` workspace is created `hidden=1` (their rule 2) and the tile "stands in" — but the tile opens the React accounts dashboard, NOT the OCR review surfaces. OCR Managers reach OCR Import / DN / Fleet Slip via `/desk/erpnext-ocr` or the sidebar, not from the landing screen. No data or permission effect; a discoverability delta at cutover. Option: ship a `Desktop Icon` **Folder** file (`erpocr_integration/desktop_icon/<label>.json`, label ≠ any workspace name) with a Link child → the workspace and an External child → `/accounts`, plus `public/icons/desktop_icons/{solid,subtle}/*.svg`; inert on v15 (no `Desktop Icon` doctype; sync never reads the folder) so it lands on **master** per ADR-032 clause 4. Same shape as fleet's; asked the coordinator (09-16 ack) whether this class gets one portfolio pattern before seats ship their own folders. Build only once a v16 cutover date exists. **2026-09-16 coordinator ruling: ONE portfolio pattern, `starpops_maintenance` ADR-0033 is the reference** — one Folder per domain shipped by the workspace-owning app; folder label ≠ every other Desktop Icon label (autoname `field:label` spans all types, so for us it must differ from both the `ERPNext OCR` workspace AND the auto-generated `OCR Accounts` App icon); Link children labelled EXACTLY the workspace name (`link_type: Workspace Sidebar`; an alias renders invisible); an External child → `/accounts` allowed; solid+subtle SVGs; lands on master. The kiosk-tile rule (tile stays top-level) does not bind erpocr — no kiosk persona holds an OCR role since 2026-08-27. **2026-09-19 correction from `starpops_maintenance` (measured on 16.33.1): an `External` child Link renders for NOBODY** (non-Folder/App icons need a `workspace_sidebar_item` of the same label). Use an **`App`-type** icon with our own `app`, `link` = `/accounts`, and a `roles` table narrowed to the roles `has_app_permission` admits. Also ship `workspace_sidebar/<name>.json` for each linked workspace, and add an `after_migrate` heal that force-imports our files over same-named non-standard rows v16 auto-creates (reference: `starpops_maintenance/install.py::heal_v16_desktop_files`, v0.3.15). | Architect + coordinator | **Open — shape ruled 2026-09-16; build deferred to v16 cutover planning** | 2026-09-16 |
| **Q17** | Adopt TypeSafe Jev as the supplier matcher's confirm step (jev_lab proposal, 2026-09-19)? | jev_lab eval #1 (Star Pops bench restore, test month 2026-05-05→06-04, 167 invoices, baseline = our v1.10.4 matcher re-implemented with a time cut): supplier top-1 **94.0% vs 84.4%**; calibrated (ECE 0.019); at a frozen threshold **62.9% of invoices at 100% precision vs our exact/alias tier's 52.1%**; <US$0.0002/invoice, ~0.5 s. The same report shows the invoice auto-draft rate does NOT improve — expense-account coding is the limit — so the gain is fewer supplier review clicks, not more drafts. **Ruling: adopt LATER, shadow-first.** (1) Shadow: record Jev's pick + probability beside today's match on both sites for ~4–6 weeks, zero behaviour change, then score against the operator-confirmed supplier on live data incl. Cactus (the eval is one site, one month, a restore). (2) Only then consider auto-confirm above a threshold tuned on that data. Hard constraints for any build: a Jev failure or timeout falls back silently to today's fuzzy tier; a MACHINE-confirmed supplier must NOT write an `OCR Supplier Alias` (a wrong pick would become a permanent tier-1 alias); the own-company guard ships in code. **Two Willie calls gate even step 1:** a new third-party processor receiving supplier names, VAT numbers and OCR aliases (POPIA / vendor — Gemini already sees the whole document, so the increment is small but it is a new processor); and auto-confirm widening automatic PI creation because `enable_auto_draft` is ON at both sites (same class as Q15). **2026-09-19 update:** Willie (via jev_lab, verbatim) — "We can do a live trial, but typesafe remains a test, it doesn't replace the current until it proves more accurate at the same or less cost" → processor gate CLEARED for a shadow-only trial. To this architect the same day: "jev is still an experiment, we can decide later" → trial allowed, NOT scheduled. Open: the definition of cost (recommended: API spend + supplier corrections per 100 invoices). Run the trial after v1.11.0 deploys, so it measures against the fixed matcher. **GO, 2026-09-19 (Willie, verbatim):** "I would like to test jev, but I don't want to replace what we have until we know jev is more accurate or the same accuracy for much cheaper". Bar as measured: top-1 supplier vs the SUBMITTED invoice's supplier, Jev vs the matcher's pick snapshotted at extraction, at import time; cost = Jev API spend vs supplier corrections per 100 invoices (the matcher has no API cost, so 'much cheaper' can only come from operator time). Shadow built as v1.12.0 (blind: permlevel-1 fields, System Manager read only; off by default; monthly spend cap; never touches supplier/status/auto-draft). Deploys AFTER v1.11.0; score after 4–6 weeks of live data on both sites. Shipped v1.12.0 (tag `f631b30` → `43ef66a`) → ADR-0023; deploy-request filed as the next hop after v1.11.0 (on DEPLOY-LIST-2026-09-19). Trial starts when Willie enters a dedicated TypeSafe key per site and enables it. | Architect (scoring) · Willie (key + enable; replace/no-replace on the data) | **Open — shadow shipped; awaiting deploy + Willie's key** | 2026-09-19 |
| **Q18** | Auto-draft "not landing": two learning holes plus zero-value lines (Willie, 2026-09-19). | Authorised read-only probe of both sites 2026-09-19. Auto-draft **fires** (SP last draft 09-18 16:30) but lands on few records: since 2026-08-01 SP 72 of 428 (17%), Cactus 12 of 112 (11%). Classified skips since 08-01 (SP / Cactus): item line Suggested with real value 156 / 37; no supplier matched 65 / 22; **supplier Suggested although the SAME printed name→supplier was accepted before 41 / 24**; supplier Suggested first time 31 / 10; **blocked only by zero-value lines 17 / 1**; totals gate 14 / 1; duplicate bill no 11 / 2. **(1) Supplier learning hole:** `OCRImport.on_update` saves an `OCR Supplier Alias` only when `supplier` changed in that save AND status is Confirmed (the JS sets Confirmed only on a field change). An operator who ACCEPTS a correct pre-filled fuzzy suggestion teaches nothing, so the same supplier returns Suggested forever (e.g. one printed name suggested the same supplier 13× since August, zero alias rows). Same shape for item rows left Suggested (47 of 312 weak rows). Candidate fix: learn at PI **submit** (the doc_events hook already fires) from the submitted supplier/lines when the record's match was Suggested — the strongest human confirmation; must not fire for auto-drafted records. **(2) Zero-value lines:** one haulier prints the return leg at R0 on every invoice; Gemini extracts it as a second line (Suggested, no account); operators delete it every time (16 records). Candidate fix: the auto-draft gate ignores lines with rate 0 AND amount 0 and the builder drops them. **(3)** the item-wording bucket is the account-conventions research (`docs/research/expense-account-rulebook-2026-09-19.html`): exact-text service mappings reach ~8% of lines; continuously-learned rules reach **45.9% at 92.6% measured at import time** (a same-day correction of an initial 65%/94.5% that leaked post-import postings; 29% of lines have no supplier history at import). **(4)** no-supplier-matched is Q17 territory. Nothing built — assessment only, awaiting Willie. **RESOLVED — Willie said "do the fixes" 2026-09-19; shipped v1.11.0 (tag `bff5398` → `affdce0`) → ADR-0022.** Deferred: correcting Auto Matched aliases that a submitted document contradicts; the item-wording bucket (research page). | Willie (ruled) | **Resolved 2026-09-19 (v1.11.0 → ADR-0022); awaiting deploy** | 2026-09-19 |
| **Q19** | Our `Fleet Vehicle` Custom Fields render inside `fleet_management`'s collapsed "Cartrack Raw Data" heading — dangling `insert_after` anchor. | Found 2026-09-21 by running the portfolio layout tripwire (`frappe-testing-unit`, landed from `starpops_production` as skills `2d2ab04`) against **live meta** on the shared dev bench, in response to the coordinator's two-checks broadcast. `install.py:150-154` plants a **collapsible** Section Break `custom_ocr_section` ("OCR Fleet Slip Settings") with `insert_after: "wesbank_cost_code"` — **no field by that name exists anywhere in the portfolio** (grepped every repo; the only occurrence is our own anchor). With the anchor unresolved Frappe appends the section at the END of the form, *after* the three fields it was meant to contain, so live meta shows `custom_fleet_card_provider` / `custom_cost_center` / `custom_fleet_control_account` sitting inside fleet_management's **collapsible** `cartrack_data_section`, and our own heading rendering empty. The canonical tripwire's second assertion (`test_every_section_break_anchor_is_the_end_of_a_section`) fails on the anchor-exists check before it reaches the layout question — independent confirmation. **Severity: discoverability only** — no value is wrong, nothing is hidden from the API, and `posting_mode` still derives correctly; but an operator configuring a vehicle must expand a heading labelled *Cartrack Raw Data* to reach the field that decides whether a slip becomes a control record or a Purchase Invoice. Mirror image of the defect class in the broadcast (ours hides nothing of theirs; theirs hides ours), and the same class `starpops_maintenance` hit with `Project.custom_factory`. **Not fixed in v1.12.0** — that tag was cut and verified before this was found, the defect predates it, and it is not worth holding a floor-blocking deploy train for a collapsed heading. **Fix shape (needs a release, not a hotfix):** re-anchor to a field that actually exists and is the last of its section, AND ship a guarded patch that rewrites the existing `Custom Field.insert_after` on live sites — a code-only fix is not enough, because `create_custom_fields(update=True)` rewrites the anchor from code on **any** app's migrate, so a data-only correction is reverted by the next unrelated deploy. The patch must read the current value, skip unless it still equals the old one (never clobber a hand-fix), then `frappe.clear_cache(doctype="Fleet Vehicle")`. **Open question for fleet_management:** was `wesbank_cost_code` a field they once shipped and dropped? If so this is a silent cross-app dependency to restore rather than an anchor to replace. **Prod ANSWERED 2026-09-21 (authorised read-only probe; Willie: "Do the read"):** two GET-only calls reading `DocField` + `Custom Field` **schema rows only** on `erp.starpops.co.za` — no Fleet Vehicle document fetched, so no registration/driver name/cost figure left prod. **`wesbank_cost_code` does not exist on prod either** (neither DocField nor Custom Field), prod's Fleet Vehicle DocField tail is identical to the bench's (52 fields, ending `cartrack_data_section` collapsible=1 → `depot` → `driver_name`), and the five erpocr Custom Field rows carry the same `insert_after` values. Same inputs → the bench's live-meta result transfers. **The worse, field-hiding variant does not exist on either site:** our section swallows nothing of fleet_management's. Live on both sites is only the inverse (our fields under their heading). Severity confirmed as discoverability-only, so this does NOT ride the 09-21 floor-blocking train — a patch that mutates `Custom Field` rows on every site does not belong bolted to another app's emergency deploy. Ships as its own version through the normal loop, **with the ADR-033 browser pass**, which a Desk layout change of all things owes. Fix anchor absent a fleet_management answer: after `driver_name`, the last field of their last section. | Architect (fix) · fleet_management (anchor history) · Willie (prod probe authorisation) | **Open — measured on dev bench 2026-09-21; prod unverified; fix deferred past v1.12.0** | 2026-09-21 |
