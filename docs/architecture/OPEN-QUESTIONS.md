# Open Questions — erpocr_integration

> Deferred calls and unresolved decisions, **each with a named owner** so they don't rot.
> When a question is answered, promote the resolution to an ADR in [DECISIONS.md](DECISIONS.md)
> and mark the row **Resolved → ADR-nnnn**. Companion to the decision register; operating model
> lives in the `architect-session` skill.
>
> **Bootstrap note (2026-07-09):** reconstructed on the first architect session from CHANGELOG,
> CLAUDE.md, handbacks, and per-session memory. DRAFT until ratified.
>
> **Owner legend:** *Willie* = MD, routes/ratifies + deploy-time UI actions · *Starktail* = hosting
> owner, runs tag→deploy · *Cactus* = the second instance's operator · *Architect* = this session's role.

---

| ID | Question / deferred call | Context & why it matters | Owner | Status | Opened |
|---|---|---|---|---|---|
| **Q1** | ~~Deploy the `v1.5.0 → v1.6.0` train; confirm prod version.~~ | **RESOLVED 2026-07-09** (Willie): Star Pops prod (`erp.starpops.co.za`) is at **v1.6.0 (HEAD)** — the `1.5.0→1.6.0` train is deployed and the 2026-07-07 request landed (superseded by v1.6.0). **Driver-shell deploy #1 is unblocked** on the erpocr side. Residual verification that the deploy's config steps ran → **Q2/Q3/Q4**. | Willie + Starktail | **Resolved 2026-07-09** | 2026-07-09 |
| **Q2** | Run **Restore Original Permissions** on `OCR Fleet Slip` on Star Pops. | **Confirmed present 2026-07-09** (read-only probe): SP still carries the shadow — **3** live Custom DocPerm rows (System Manager pl0, OCR Manager pl0, OCR Fleet Slip Reader pl0) vs the shipped **5** (adds **System Manager pl=1** for `raw_payload` + **OCR Fleet Driver**). So `raw_payload` is still unreadable; the driver-role row is masked but D0 (ADR-0006) makes that harmless for upload. **Cactus = 0 rows (clean).** Action: Customize Form → OCR Fleet Slip → Restore Original Permissions (Willie; Starktail can't — needs the UI). | Willie (UI action) | **Open — confirmed, restore owed (SP)** | 2026-07-09 |
| **Q3** | Set `OCR Settings → Import (Actual VAT) Tax Template = "9 - Import with Std VAT"` on **Cactus**. | **Confirmed 2026-07-09** (read-only probe): Cactus **is** at v1.6.0 (code present), but `import_tax_template` is **unset (None)** → ADR-0008's customs-VAT fix is **inert on Cactus**. Until set, Cargo Compass import VAT is still mis-posted and hand-corrected. | Cactus / Willie | **Open — config owed (Cactus)** | 2026-07-09 |
| **Q4** | Confirm auto-draft produces a **correct** draft end-to-end, now that v1.6.0 is live. | **FY-guard fix VERIFIED live 2026-07-09** (read-only probe): **0** Fiscal-Year rejections among the 4 records processed post-deploy (modified ≥ 07-07); the 12 "outside any active Fiscal Year" skips are all **pre-deploy fossils** (last modified ≤ 2026-07-02) and SP has active FYs covering those dates (FY 2026-2027 open). **Still pending:** only **2** `auto_drafted=1` ever; post-deploy volume (winter break) is 4 *low-confidence* records (no-supplier / 'Suggested'), so a *correct high-confidence draft* is still unproven — verify when real volume resumes. Cactus customs-PI verification gated on Q3. | Architect + Willie | **Open — fix verified; correctness pending volume** | 2026-07-09 |
| **Q5** | Finalize + merge the `starpops_accounts` fold-in (ADR-0010). | **UAT clear (Willie 2026-07-09): Danell completed UAT, nothing surfaced** — the gate is removed. Worktree `../erpocr-foldin` (branch `feature/starpops-accounts-foldin`) is **~50 commits behind master with uncommitted WIP** — needs rebase onto master + finalize before merge; the fold-in PR **must flag the Starktail image-build Node step**. Ready to execute — likely a builder kickoff. | Architect (kickoff) + Willie | **Open — ready to execute (rebase + merge)** | 2026-07-09 |
| **Q6** | Remove the **vestigial `custom_fleet_control_account`** capture on the Fleet Card path. | Since v1.2.0 (ADR-0003) no PI is created on Fleet Card slips, so the control account captured per slip via `_apply_vehicle_config` no longer flows anywhere — informational only. CLAUDE.md flags "cleanup TBD in a future release." Low priority; a clean-up kickoff when a fleet-touching release is already open. | Architect | **Open — backlog** | 2026-07-09 |
| **Q7** | Small deferred review items (non-blocking, from the 2026-07-06 live review). | (a) **Fleet pipeline has no Actual-injection** — an operator manually picking the import template on a fleet slip gets a 0-tax row; (b) **decode-gate only on `upload_fleet_slip`**, not on manual/email/Drive image ingest; (c) **item aliases are globally keyed** — supplier-scoped aliases are the durable fix if cross-supplier description collisions bite; (d) **`getdate()` hardening** on recon date compares. | Architect | **Open — backlog** | 2026-07-09 |
| **Q8** | Is manual **Mark Recorded** the right long-term UX for Fleet Card slips, or should high-confidence ones auto-record? | **34+ Fleet Card slips sat in `Matched`** awaiting manual Mark Recorded as of the 2026-07-06 review (was ~17 at the v1.2.0 audit) — a growing operational backlog. Auto-recording touches the ADR-0003 invariant and the `fleet_management` reconciliation contract, so it's a business/process call, not a silent code change. | Willie (process) + Architect | **Open — process question** | 2026-07-09 |
